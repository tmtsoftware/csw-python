This directory stores protobuf generated files for scalapb (used by CSW).  To construct the files here, use:

``
protoc --python_out=.. scalapb/*.proto 
``

from the $<PROJECT_ROOT>/protobuf directory.
